#include<bits/stdc++.h>
using namespace std;
int main(){

for (int i = 0; i <4; i++)
{
    for (int j = 0; j <4; j++)
    {
        cout<<"*";
    }
    cout<<endl;
}

/*  here the outer and inner loop i have figured out but
i kept struggle to use endl inside the inner loop ,
so we all use the endl outside the inner loop so as it completes that iteration 
it goes to endl and goes to another line to make the square
*/




    return 0;
}